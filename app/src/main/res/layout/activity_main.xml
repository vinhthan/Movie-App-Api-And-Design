<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.movies.MainActivity"
    android:id="@+id/drawerMenu">

    <include android:id="@+id/main"
        layout="@layout/layout_main"/>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        app:headerLayout="@layout/header"
        app:menu="@menu/menu_drawer"
        android:layout_gravity="start"

        app:itemTextColor="@color/colorViolet"
        app:itemIconTint="@color/colorViolet"
        android:background="@color/colorWhile"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"/>

</androidx.drawerlayout.widget.DrawerLayout>

<!--
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    Thành phần con thứ nhất (giao diện ứng dụng)

    Thành phần con thứ hai (left menu)

    Bắt buộc phải theo thứ tự như trên

    </android.support.v4.widget.DrawerLayout>
-->